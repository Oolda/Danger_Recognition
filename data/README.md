# Data

This folder contains processed feature sequences and ground-truth labels used for model training and evaluation.

- `X_train_sequences.npy`: training temporal feature sequences
- `X_test_sequences.npy`: testing temporal feature sequences
- `y_true_binary.npy`: sequence-level ground-truth labels derived from XML annotations
